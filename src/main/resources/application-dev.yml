spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5433/cafe
    username: postgres
    password: 123456789
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect

# Loads worked-example demo data on startup (dev only).
app:
  seed:
    enabled: true
